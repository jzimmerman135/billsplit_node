<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <%- include('../partials/_header') %>
        <title>Billsplit - Example</title>
        <!-- LINKS TO EXTERNAL STYLESHEETS
            format: rel="/stylesheets/example.css" -->
        <style>
            .message {
                font-family: 'Roboto';
                margin-left: auto;
                margin-right: auto;
                justify-self: center;
                color: var(--text_main);
                opacity: 1;
                font-size: 30px;
                text-align: center;
            }
            .container div {
                padding: 1rem;
                margin: auto;
                max-width: 40ch;
                display: flex;
                flex-direction: column;
            }

            .return {
                margin: auto;
                margin-top: 1rem;
                padding: 0.25rem 0.8rem;
                font-size: 20px;
                opacity: 0.9;
                background-color: var(--text_dark);
                color: var(--text_light);
                font-family: "Roboto";
                text-decoration: none;
                border-radius: 0.5rem;
                border: 2px solid var(--text_dark);
                transition: 0.5s;
            }

            .return:hover {
                background-color: var(--bg_purple);
            }
        </style>
</head>

<body>
    <div id="pageContainer">
        <!-- HEADER STUFF, CAN IGNORE -->
        <%- include('../partials/_nav') %>
            <!-- ALL PAGE CONTENT INSIDE HERE -->
            <div class="container" style="margin-top: 2.5rem;">
                <div>
                <h2 class="message"><%= line1 %></h2>
                <br>
                <h2 class="message"><%= line2 %></h2>
                <a class="return" href="<%= returnHREF %>">Return to <%= returnWhere %></a>
                </div>
            </div>

            <!-- FOOTER CAN IGNORE -->
            <%- include('../partials/_footer') %>
    </div>

</body>
<!-- get username information, sorry it's named guestUser
            gives access to set and delete username functions -->


    <!-- LINKS TO EXTERNAL FUNCTION SCRIPTS
         format: src"/scripts/example.js" -->
    <script src="/scripts/guestUserLogic.js"></script>
    <!-- Page Executable -->
    <script>
        //vscode thinks there is a problem here, but there is not!
        var truth = "<%= saveUsernameCookie %>"
        if ( truth == "true" ) {
            console.log("MAKING COOKIE");
            storeUsername( "<%= username %>" );
        }
    </script>

    <%- include('../partials/_guestUser') %>



</html>